package shit.zen.value;

public enum ValueType {
    GROUP,
    TOGGLE_GROUP,
    MODE_GROUP,
    BOOLEAN,
    INTEGER,
    DECIMAL,
    TEXT,
    ENUM,
    MULTI_ENUM,
    COLOR,
    GRADIENT,
    INT_RANGE,
    DECIMAL_RANGE,
    KEY_BIND,
    VEC2,
    VEC3,
    ITEM,
    BLOCK,
    ENTITY_TYPE,
    SOUND_EVENT,
    FILE,
    RESOURCE_LOCATION
}
